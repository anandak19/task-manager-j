import { QuillTextPipe } from './quill-text-pipe';

describe('QuillTextPipe', () => {
  it('create an instance', () => {
    const pipe = new QuillTextPipe();
    expect(pipe).toBeTruthy();
  });
});
